# Runtime dependency of the REAL client only. The bridge itself, --stub mode,
# and the whole test suite run on the Python standard library alone.
#
# Floor rationale: the bridge relies on `strict: true` tool definitions,
# forced tool_choice, cache_control system blocks, usage.cache_*_input_tokens,
# and the claude-opus-4-8 / claude-sonnet-4-6 model generation -- all of which
# the >=0.92 SDK line is documented against. 0.40-era SDKs predate strict
# tool use and are untested with this bridge.
anthropic>=0.92

# Dev/test dependencies (install manually if needed):
# pytest>=8
